import 'package:flutter/material.dart';

class TermsPrivate extends StatelessWidget {
  const TermsPrivate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('이용 약관')),
      body: Column(children: [Text('')]),
    );
  }
}
